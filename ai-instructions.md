# AI Developer Rules & Instructions 📜

ไฟล์นี้เป็นข้อตกลงและกฎเหล็กที่ AI ทุกตัว (Gemini, Claude, GPT) ต้องปฏิบัติตามอย่างเคร่งครัดในการร่วมพัฒนาโปรเจกต์นี้ หากฝ่าฝืนจะถือว่าทำงานผิดพลาด

---

## 🚨 1. กฎเหล็กเรื่องการสื่อสารและการตัดสินใจ (Communication Rules)
* **ห้ามเดา ห้ามสุ่ม:** หากข้อมูลไม่ชัดเจน ไม่เข้าใจความต้องการ หรือโค้ดส่วนไหนมีปัญหา ให้หยุดและถามผู้ใช้เพื่อความแน่ใจทันที ห้ามเดาสุ่มเขียนไปเอง
* **ถามก่อนเขียนโค้ดเสมอ:** ก่อนจะเริ่มพิมพ์โค้ดชุดใหม่ หรือขึ้นระบบใหม่ ให้สรุปแนวคิด ยืนยันสถาปัตยกรรม หรืออธิบายวิธีที่จะทำคร่าวๆ ให้ผู้ใช้ฟังและอนุมัติก่อนเสมอ
* **ห้ามปรับปรุงโค้ดส่วนอื่นตามใจชอบ:** ห้ามแตะต้อง แปะโค้ดทับ หรือแก้ไขโค้ดในไฟล์หรือฟังก์ชันที่ไม่เกี่ยวข้องกับคำสั่งปัจจุบันเด็ดขาด
* **ต้องขออนุญาตก่อน Refactor:** หากเห็นว่าโค้ดเก่ามีจุดบกพร่องและจำเป็นต้องปรับปรุงโครงสร้าง (Refactor) ต้องแจ้งเหตุผลและขออนุญาตจากผู้ใช้ก่อนเริ่มลงมือเสมอ

---

## 💻 2. มาตรฐานการเขียนโค้ด (Coding Standards)

### 🎨 ฝั่งหน้าบ้าน (Angular)
* ต้องเขียนในรูปแบบ **Standalone Components** เท่านั้น (ห้ามใช้ NgModule แบบโบราน)
* ควบคุม Type ของข้อมูลให้เป๊ะ 100% โดยอิงตาม Interface ที่กำหนดไว้ใน `core/models/` ห้ามใช้ `any` เด็ดขาด
* การจัดการสไตล์ ต้องใช้คลาสของ **Tailwind CSS** เป็นหลัก หลีกเลี่ยงการเขียน CSS ดิบในไฟล์ `.css` ถ้าไม่จำเป็นจริงๆ
* ถ้าต้องการแต่ง UI ต่าง ๆ พยายามอย่าใช้ อิโมจิ ให้ใช่ไอค่อนที่มีการ import มาเสมอ

### 🛡️ ฝั่งหลังบ้าน (.NET Core / C#)
* ต้องใช้สถาปัตยกรรม **Minimal APIs** ในการสร้าง Endpoint (`app.MapGet`, `app.MapPost`) โค้ดต้องกระชับ ไม่เยิ่นเย้อ
* การตั้งชื่อตัวแปร ฟังก์ชัน และคลาสใน C# ให้ใช้รูปแบบ **PascalCase** เป็นหลัก (เช่น `GetPrompts`, `PositivePrompt`)
* การเชื่อมต่อฐานข้อมูลต้องทำผ่าน **Entity Framework Core (EF Core)** และเขียน Query ด้วย LINQ เท่านั้น

---

## 📦 3. รูปแบบการส่งมอบโค้ด (Code Delivery Rules)
* **ห้ามซ่อนโค้ดย่อ (No Placeholders):** เวลาเขียนโค้ดให้ดู ห้ามใช้คอมเมนต์มักง่ายประเภท `// ... โค้ดเดิมของคุณ ...` หรือ `/* TODO: เขียนต่อเอง */` ให้แสดงโค้ดเต็มไฟล์ หรือระบุจุดที่ให้แทนที่อย่างชัดเจน
* **อธิบายสั้นกระชับ:** เมื่อส่งโค้ดเสร็จแล้ว ให้สรุปสั้นๆ ว่าแก้ไขตรงไหนไปบ้างและเพราะอะไร ไม่ต้องเกริ่นยาว เน้นเนื้อหาที่เป็นเทคนิคล้วนๆ
* **ตรวจสอบผลกระทบ (Side Effects):** ทุกครั้งที่เสนอโค้ดใหม่ ต้องตรวจสอบเสมอว่าโค้ดนั้นจะไม่ไปทำให้ระบบ Routing หรือระบบ Database Migration ที่มีอยู่เดิมพัง

---

## 🎯 4. บริบทและจุดประสงค์ที่ต้องจำ
* โปรเจกต์นี้เป็นแอปพลิเคชันเก็บ **Stable Diffusion Prompt พร้อมรูปภาพผลงาน**
* เป้าหมายสูงสุดของผู้ใช้คือ **"ต้องการฝึกหัดภาษา .NET (C#) และ Angular"** ดังนั้น AI ต้องช่วยอธิบายคอนเซปต์เบื้องหลังของโค้ดที่เขียน เพื่อให้ผู้ใช้เข้าใจและนำไปประยุกต์ใช้ต่อได้จริง ไม่ใช่แค่ก๊อปวางเสร็จแล้วจบไป